= form_for @question do |f|
  p
    = f.label :title
    = f.text_field :title
  p
    = f.label :body
    = f.text_area :body
  h4 Attachments:
  = render 'attachments/form', f: f

  p= f.submit 'Create'
